import React from 'react';

// objectType: (char) 
        // c - clef
        // t - time
        // k - key
        // b - barline
        // n - note

        // > if time
        // numerator: (int) up 
        // denominator: (int) down

const Time = (obj) => {
    return (
        <div>
        </div>
    );
};

export default Time;