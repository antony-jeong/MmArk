import React from 'react';
import PropTypes from 'prop-types';

const Logo = ({className}) => {
    return (
        <div className={`${className}`}>
            Logo
        </div>
    );
};

Logo.propTypes = {
    
};

export default Logo;