import React from 'react';

const Checker = ({type, answer}) => {
    // if type is input, checkAnswer is string
    
    // switch (type) {
    //     case "" :
    // }

    return (
        <div>
            
        </div>
    );
};

export default Checker;